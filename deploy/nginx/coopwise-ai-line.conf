limit_req_zone $binary_remote_addr zone=line_webhook:10m rate=20r/s;

server {
    listen 80;
    listen [::]:80;
    server_name line.example.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name line.example.com;

    ssl_certificate /etc/letsencrypt/live/line.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/line.example.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 2m;

    location = /webhooks/line {
        limit_req zone=line_webhook burst=40 nodelay;
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout 15s;
    }

    location = /health/live {
        proxy_pass http://127.0.0.1:8000;
        access_log off;
    }

    # Message-send endpoints are intentionally not exposed through public Nginx.
    location / {
        return 404;
    }
}

